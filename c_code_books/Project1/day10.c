#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

/*
打印图案的练习，该任务是打印用“*”组成的“空心”正方形图案。

输入描述：
多组输入，一个整数（3~20），表示输出的行数，也表示组成正方形边的“*”的数量。

输出描述：
针对每行输入，输出用“*”组成的“空心”正方形，每个“*”后面有一个空格。使用C语言编写
*/
//int main() {
//    int n;
//    // 处理多组输入，直到输入结束
//    while (scanf("%d", &n) != EOF) {
//        // 打印第一行（全星号）
//        for (int i = 0; i < n; i++) {
//            printf("* ");
//        }
//        printf("\n");
//
//        // 打印中间行（空心部分）
//        for (int i = 1; i < n - 1; i++) {
//            printf("* "); // 每行第一个星号
//            // 打印中间的空格
//            for (int j = 1; j < n - 1; j++) {
//                printf("  "); // 两个空格，对应一个星号加一个空格的宽度
//            }
//            printf("*\n"); // 每行最后一个星号
//        }
//
//        // 打印最后一行（全星号）
//        if (n > 1) { // 防止n=1时重复打印，题目中n≥3，此判断为鲁棒性设计
//            for (int i = 0; i < n; i++) {
//                printf("* ");
//            }
//            printf("\n");
//        }
//    }
//    return 0;
//}


// VS 实用调试技巧
// 什么是 BUG ？现在一般指在电脑系统或程序中，隐藏着的一些未被发现的缺陷或问题，简称 程序漏洞
// 什么是调试 ？这个找问题的过程叫称为调试，英文叫 debug（消灭bug）的意思

// Debug 和 Release
// Debug：通常称为调试版本，包含调试信息，不做任何优化，便于程序员调试程序
// Release：为发布版本，(.exe)大小比 debug 小。不能调试

// 调试快捷键 
// F9：创建断点，取消短点
// 断点的作用是可以在任意的位置设置断点，打上断点就可以使得程序执行到想要的位置暂停执行，
// 接下来我们就可以使用 F10，F11 这些快捷键，观察代码的执行细节。
// F5：启动调试，经常用来直接跳转到下一个断点处
// F10：逐过程。用来处理一个过程，可以是函数的调用，一次语句
// F11：逐语句。每次都执行一个语句

// 条件断点：达到某个条件

// Ctrl + F5：执行不调试

//int main()
//{
//	int arr[10] = { 0 };
//	int i = 0;
//	for (i = 0; i < 10; i++)
//	{
//		arr[i] = i;
//	}
//	for (i = 0; i < 10; i++)
//	{
//		printf("%d ", arr[i]);
//	}
//	return 0;
//}

// 调试举例1
// 求 1！+ 2！+ 3！+ 4！+...+ n!
//int main()
//{
//	int n = 0;
//	int ret = 1;
//	int sum = 0;
//	scanf("%d", &n);
//	for (int n = 1; n <= 10; n++)
//	{
//		for (int i = 1; i <= n; i++)
//		{
//			ret *= i;
//		}
//		sum += ret;
//	}
//	printf("%d\n", sum);
//	return 0;
//}

// 修正版
//int main()
//{
//    int n = 0;
//    long long sum = 0;  // 改用long long防止溢出
//    scanf("%d", &n);
//
//    for (int i = 1; i <= n; i++)  // 外层循环变量改名i，避免重名
//    {
//        long long ret = 1;  // 每次计算新阶乘前，必须重置ret为1
//        for (int j = 1; j <= i; j++)
//        {
//            ret *= j;
//        }
//        sum += ret;
//    }
//
//    printf("%lld\n", sum);  // long long类型用%lld输出
//    return 0;
//}

// 优化版
//int main()
//{
//    int n = 0;
//    long long ret = 1;  // 保存当前阶乘结果
//    long long sum = 0;
//    scanf("%d", &n);
//
//    for (int i = 1; i <= n; i++)
//    {
//        ret *= i;       // 直接利用上一次的阶乘结果计算当前阶乘
//        sum += ret;
//    }
//
//    printf("%lld\n", sum);
//    return 0;
//}

// 运行时错误：依赖于经验


// 函数递归
// 递归是什么？
// 解决问题的方法，在C语言中，递归就是自己调用自己。
// 递归的思想：把大事化小的过程。
// 递归中的递就是 递推 的意思，归就是 回归 的意思

// 最简单的递归
// 这是一个错误的递归
//int main()
//{
//	printf("hehe\n");
//	main();// 频繁向栈区申请空间，栈区内存会溢出
//	return 0;
//}

// 递归的限制条件
// 2个必要条件：
// ·递归存在限制条件，当满足这个限制条件的时候，递归便不再继续。
// ·每次递归调用之后越来越接近这个限制条件。

// 举例：求 n 得阶层
//int Fac(int n)
//{
//	if (n == 0)
//	{
//		return 1;
//	}
//	else
//	{
//		return Fac(n - 1) * n;
//	}
//}
//int main()
//{
//	int n = 0;
//	scanf("%d", &n);
//	int r = Fac(n);
//	printf("%d\n", r);
//	return 0;
//}







